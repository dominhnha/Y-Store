import React from 'react'
import PropTypes from 'prop-types'
import { Outlet } from 'react-router-dom'

const ProfileInfo = props => {
    console.log("clear 11")
  return (
    <Outlet>
      oke
    </Outlet>
  )
}

ProfileInfo.propTypes = {}

export default ProfileInfo